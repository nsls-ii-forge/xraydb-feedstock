{% set name = "xraydb" %}
{% set version = "4.3" %}
{% set sha256 = "cf1bfd80eb21c90710f1ca9c1c7131174ad3a409a2f61e12fefb8532e9459d22" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  host:
    - pip
    - python
  run:
    - python
    - numpy
    - scipy
    - sqlalchemy

test:
  imports:
    - xraydb
  requires:
    - pytest
  source_files:
    - tests
  commands:
    - pytest -vvvv

about:
  home: https://xraypy.github.io/XrayDB
  license: PUBLIC-DOMAIN
  license_family: PUBLIC-DOMAIN
  license_file: LICENSE
  summary: X-ray Reference Data for the Elements
  doc_url: https://xraypy.github.io/XrayDB
  dev_url: https://github.com/xraypy/XrayDB
